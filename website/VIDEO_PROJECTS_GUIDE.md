# 视频项目路径规则使用指南

## 📖 概述

本指南说明如何在官网中添加和管理视频项目展示。每个文件夹代表一个项目，可以包含多个介绍视频。

## 🎯 设计理念

- **简单直观**: 文件夹即项目，无需复杂配置
- **灵活扩展**: 支持多视频、多标签、外部链接
- **开发友好**: 自动静态生成，无需手动路由配置
- **性能优化**: 符号链接方式，避免文件复制

## 📂 目录结构

```
website/
├── content/video/              # 视频内容根目录
│   ├── 奇绩创坛/               # 示例项目
│   │   ├── metadata.json       # 项目配置文件 ⭐
│   │   └── 奇绩创坛typewriter.mp4  # 视频文件
│   └── 新项目/                 # 你的新项目
│       ├── metadata.json
│       ├── video1.mp4
│       └── video2.mp4
│
├── public/
│   └── content -> ../content   # 符号链接（自动创建）
│
└── src/
    ├── app/projects/videos/
    │   ├── page.tsx            # 列表页: /projects/videos
    │   └── [slug]/
    │       └── page.tsx        # 详情页: /projects/videos/[slug]
    └── lib/
        └── videoProjects.ts    # 工具函数
```

## 🚀 快速开始

### 1. 创建新项目

```bash
# 在 content/video/ 下创建新文件夹
mkdir -p content/video/我的新项目
```

### 2. 添加 metadata.json

在项目文件夹中创建 `metadata.json`：

```json
{
  "name": "我的新项目",
  "slug": "my-new-project",
  "description": "这是一个精彩的项目介绍",
  "coverImage": "/images/projects/my-project-cover.jpg",
  "status": "进行中",
  "tags": ["AI", "Web开发", "创新"],
  "videos": [
    {
      "title": "项目演示视频",
      "filename": "demo.mp4",
      "description": "展示项目的核心功能",
      "duration": "3分钟",
      "thumbnail": "/images/projects/demo-thumb.jpg"
    },
    {
      "title": "技术实现讲解",
      "filename": "tech-details.mp4",
      "description": "深入讲解技术架构",
      "duration": "5分钟"
    }
  ],
  "links": {
    "website": "https://example.com",
    "github": "https://github.com/username/repo"
  },
  "createdAt": "2026-02-03",
  "updatedAt": "2026-02-03"
}
```

### 3. 添加视频文件

将视频文件放入项目文件夹，确保文件名与 `metadata.json` 中的 `filename` 匹配：

```bash
# 复制或移动视频文件
cp ~/Downloads/demo.mp4 content/video/我的新项目/
cp ~/Downloads/tech-details.mp4 content/video/我的新项目/
```

### 4. 访问页面

开发服务器会自动识别新项目：

- 列表页: `http://localhost:3000/projects/videos`
- 详情页: `http://localhost:3000/projects/videos/my-new-project`

## 📋 metadata.json 字段详解

### 必需字段

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `name` | string | 项目中文名称 | "奇绩创坛" |
| `slug` | string | URL标识符（英文，小写，用连字符） | "qiji-creation-camp" |
| `description` | string | 项目简介（50-200字） | "奇绩创坛项目介绍视频" |
| `status` | string | 项目状态：`进行中`/`规划中`/`已完成` | "已完成" |
| `tags` | array | 标签数组（3-5个为宜） | ["创业", "孵化器"] |
| `videos` | array | 视频列表（见下方Video对象） | [...] |
| `createdAt` | string | 创建日期（YYYY-MM-DD格式） | "2026-01-15" |
| `updatedAt` | string | 最后更新日期 | "2026-02-03" |

### 可选字段

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `coverImage` | string | 封面图片路径 | 🎬图标 |
| `links.website` | string | 项目官网链接 | - |
| `links.github` | string | GitHub仓库链接 | - |

### Video 对象

每个视频对象包含以下字段：

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `title` | string | ✅ | 视频标题 |
| `filename` | string | ✅ | 视频文件名（需与实际文件匹配） |
| `description` | string | ✅ | 视频描述 |
| `duration` | string | ❌ | 视频时长（如"3分钟"） |
| `thumbnail` | string | ❌ | 视频缩略图路径 |

## 🎨 最佳实践

### Slug 命名规范

- ✅ 使用小写字母
- ✅ 单词间用连字符分隔
- ✅ 使用英文或拼音
- ❌ 避免使用空格、特殊字符、中文

```json
// ✅ 好的示例
"slug": "qiji-creation-camp"
"slug": "ai-code-assistant"
"slug": "knowledge-base-v2"

// ❌ 不好的示例
"slug": "奇绩创坛"           // 包含中文
"slug": "My New Project"    // 包含空格和大写
"slug": "project_123"       // 使用下划线
```

### 视频文件建议

**格式与编码**
- 格式: MP4 (推荐)
- 编码: H.264
- 音频: AAC

**分辨率与质量**
- 推荐分辨率: 1920x1080 (Full HD) 或 1280x720 (HD)
- 比特率: 2-5 Mbps
- 帧率: 30fps 或 60fps

**文件大小**
- 单个视频: < 100MB (推荐 < 50MB)
- 如果文件太大，考虑压缩或分段

**文件命名**
- ✅ 使用英文或拼音
- ✅ 描述性命名: `demo.mp4`, `tech-intro.mp4`
- ❌ 避免特殊字符和空格

### 标签策略

选择3-5个最相关的标签：

```json
"tags": ["AI Agent", "LLM", "自然语言处理"]  // ✅ 适中，具体
"tags": ["AI"]                               // ❌ 太少
"tags": ["AI", "机器学习", "深度学习", "NLP", "GPT", "LLM", "Agent", "RAG"]  // ❌ 太多
```

### 描述撰写

- 项目描述: 50-200字，说明项目是什么
- 视频描述: 20-50字，说明该视频展示什么

```json
{
  "description": "基于大语言模型的智能车载助手，提供语音交互、导航规划、信息查询等功能。",  // ✅
  "videos": [
    {
      "description": "展示语音控制和导航功能的实际使用场景"  // ✅
    }
  ]
}
```

## 🔧 常见操作

### 更新现有项目

1. 编辑 `metadata.json`
2. 更新 `updatedAt` 字段为当前日期
3. 刷新页面查看更改

```bash
# 编辑metadata
nano content/video/项目名/metadata.json

# 测试更改
npm run dev
```

### 添加新视频到现有项目

1. 将视频文件复制到项目文件夹
2. 在 `metadata.json` 的 `videos` 数组中添加新条目

```json
{
  "videos": [
    {
      "title": "原有视频",
      "filename": "old-video.mp4",
      "description": "..."
    },
    {
      "title": "新添加的视频",  // 新增
      "filename": "new-video.mp4",
      "description": "展示新功能"
    }
  ]
}
```

### 删除项目

直接删除项目文件夹即可：

```bash
rm -rf content/video/要删除的项目
```

### 项目排序

项目按 `updatedAt` 降序排列（最新更新的排在前面）。要调整顺序，修改 `updatedAt` 字段。

## 🧪 测试与验证

### 运行测试脚本

```bash
node scripts/test-video-projects.js
```

输出示例：
```
🧪 测试视频项目路径配置

✅ 测试1: 检查content/video目录
   ✓ content/video 目录存在

✅ 测试2: 读取所有项目
   找到 2 个项目目录:
   - 奇绩创坛
   - 我的新项目

✅ 测试3: 验证metadata.json
   ✓ 奇绩创坛: metadata.json 有效
   ✓ 我的新项目: metadata.json 有效

✅ 测试4: 检查视频文件
   ✓ 奇绩创坛/奇绩创坛typewriter.mp4 - 144.39 MB
   ✓ 我的新项目/demo.mp4 - 25.5 MB

✅ 测试5: 检查public/content符号链接
   ✓ 符号链接存在

🎉 所有测试通过！
```

### 手动验证清单

- [ ] metadata.json 格式正确（可用 JSON validator）
- [ ] 所有必需字段都已填写
- [ ] slug 使用英文且唯一
- [ ] 视频文件名与 filename 字段匹配
- [ ] 视频文件可以播放
- [ ] 图片路径正确（如有）
- [ ] 外部链接可访问（如有）

## 🚢 生产环境部署

### 方式1: 符号链接（推荐）

构建时确保符号链接存在：

```bash
# 在 CI/CD 中添加
cd public && ln -s ../content content
npm run build
```

### 方式2: 复制文件

在 `package.json` 中添加预构建脚本：

```json
{
  "scripts": {
    "prebuild": "mkdir -p public/content && cp -r content/video public/content/",
    "build": "next build"
  }
}
```

### 方式3: CDN托管

将视频上传到CDN，metadata中使用完整URL：

```json
{
  "videos": [
    {
      "filename": "https://cdn.example.com/videos/demo.mp4"
    }
  ]
}
```

需修改 `src/lib/videoProjects.ts` 中的 `getVideoPath` 函数：

```typescript
export function getVideoPath(projectSlug: string, filename: string): string {
  // 如果是完整URL，直接返回
  if (filename.startsWith('http://') || filename.startsWith('https://')) {
    return filename;
  }

  // 否则使用本地路径
  // ... 现有逻辑
}
```

## 🐛 故障排查

### 问题：视频无法播放

**可能原因**:
1. 视频格式不支持
2. 文件路径错误
3. 符号链接未创建

**解决方案**:
```bash
# 检查视频格式
file content/video/项目名/视频.mp4

# 检查符号链接
ls -la public/content

# 重新创建符号链接
cd public && rm -f content && ln -s ../content content

# 验证视频可访问
curl -I http://localhost:3000/content/video/项目名/视频.mp4
```

### 问题：项目不显示在列表中

**可能原因**:
1. metadata.json 格式错误
2. 缺少必需字段
3. 项目文件夹名称问题

**解决方案**:
```bash
# 验证 JSON 格式
cat content/video/项目名/metadata.json | jq .

# 运行测试脚本
node scripts/test-video-projects.js

# 检查开发服务器日志
npm run dev
```

### 问题：页面404

**检查**:
1. slug 是否正确
2. 路由文件是否存在
3. Next.js 是否正确编译

```bash
# 检查路由
ls -la src/app/projects/videos/[slug]/

# 重启开发服务器
npm run dev

# 清除缓存
rm -rf .next
npm run dev
```

## 📚 示例项目

### 示例1: 单视频项目

```json
{
  "name": "简单演示",
  "slug": "simple-demo",
  "description": "一个简单的项目演示",
  "status": "已完成",
  "tags": ["演示", "教程"],
  "videos": [
    {
      "title": "项目演示",
      "filename": "demo.mp4",
      "description": "展示项目核心功能"
    }
  ],
  "createdAt": "2026-02-03",
  "updatedAt": "2026-02-03"
}
```

### 示例2: 多视频项目

```json
{
  "name": "完整项目介绍",
  "slug": "full-project-intro",
  "description": "包含多个视频的完整项目介绍",
  "coverImage": "/images/projects/cover.jpg",
  "status": "进行中",
  "tags": ["AI", "产品", "技术"],
  "videos": [
    {
      "title": "项目概览",
      "filename": "overview.mp4",
      "description": "项目整体介绍",
      "duration": "2分钟",
      "thumbnail": "/images/projects/thumb1.jpg"
    },
    {
      "title": "核心功能演示",
      "filename": "features.mp4",
      "description": "详细展示核心功能",
      "duration": "5分钟",
      "thumbnail": "/images/projects/thumb2.jpg"
    },
    {
      "title": "技术架构讲解",
      "filename": "architecture.mp4",
      "description": "深入技术实现细节",
      "duration": "8分钟",
      "thumbnail": "/images/projects/thumb3.jpg"
    }
  ],
  "links": {
    "website": "https://project.example.com",
    "github": "https://github.com/username/project"
  },
  "createdAt": "2026-01-15",
  "updatedAt": "2026-02-03"
}
```

## 🔗 相关链接

- [Next.js 动态路由文档](https://nextjs.org/docs/app/building-your-application/routing/dynamic-routes)
- [HTML5 Video 元素](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/video)
- [视频优化最佳实践](https://web.dev/fast/#optimize-your-images)

## 📞 技术支持

如有问题，请查看：
1. 本文档的故障排查部分
2. 运行测试脚本获取详细信息
3. 检查开发服务器控制台输出

---

最后更新: 2026-02-03
